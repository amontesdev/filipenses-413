# Crear/copiar a ~/.filipenses-backup/.env con permisos 600 (NO subir al repo).
CLOUD_REF='phcpmgfpuobeyptbbgcy'
SUPABASE_ACCESS_TOKEN='sbp_...'   # PAT de Supabase (nivel cuenta)
LOCAL_DB_PASSWORD='<password postgres local>'
# Nota: el backup usa la Management API (PAT); NO necesita la contraseña del Postgres cloud.
